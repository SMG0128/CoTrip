# 与 AI 共生 · 多人行程协同小程序
## V1 产品定义与开发基线

> **核心理念：大家负责表达想法，AI 负责把想法变成共同计划。**

---

## 1. 项目定位

本项目是一款面向多人线下活动场景的微信小程序。

传统多人出行或活动规划中，参与者往往会在群聊中不断提出碎片化需求：

- “我早上 10 点之后才有空。”
- “我只想在天河区打羽毛球。”
- “吃饭可以去越秀。”
- “最近没什么钱，能不能便宜一点？”
- “我下午 5 点之前一定要走。”

这些信息通常散落在聊天记录中，需要某一个人手动整理、协调、搜索地点、重新调整时间。

本项目通过 AI 将参与者的自然语言需求转换为结构化约束，并持续维护一个可执行的共同计划。

AI 不作为传统聊天机器人存在，而作为整个行程背后的 **多人意图协调层**。

---

# 2. 核心产品逻辑

```text
参与者提出自然语言需求
        ↓
AI 解析需求
        ↓
结构化 Constraint
        ↓
冲突检测 / 计划重排
        ↓
真实世界 Provider
        ↓
Time / Location / Restaurant / Price / Route
        ↓
共同计划
        ↓
个人出发建议
        ↓
通知提醒
```

核心原则：

> **群聊负责产生想法，AI 负责维持共识。**

---

# 3. 页面结构

## 3.1 首页

首页保持极简，只保留核心入口。

### 顶部功能

- 新建行程
- 历史行程

### 主窗口

大窗口根据当前状态动态展示：

#### 存在正在进行的行程

展示：

- 行程名称
- 参与人数
- 当前主要事件
- AI 已整理需求数量
- 是否存在冲突
- 进入行程按钮

示例：

```text
周六羽毛球 + 越南菜

4 人参与 · AI 已整理 8 条需求

11:00  羽毛球
   ↓
14:00  越南菜

还有 1 项需求待确认

进入行程 →
```

#### 当前没有正在进行的行程

展示推荐组合，例如：

```text
🏸 + 🍲

下午打球，晚上吃饭
天河 · 约 ¥80 / 人

以此新建行程
```

推荐的目标不是构建内容平台，而是推动用户产生：

> “要不要一起去？”

的活动意图。

---

# 4. 新建行程

新建页面只负责提供行程的初始骨架，不要求创建者一次性填写完整计划。

## 4.1 区域限定

可选。

支持：

- 不限区域
- 指定地点
- 指定行政区域
- 地图范围
- 后续扩展地图圈选

示例：

```text
广州市天河区
```

区域不是纯文本，应保存为结构化 Location / Area 数据。

---

## 4.2 时间范围

支持：

- 明确日期 + 起止时间
- 某一天
- 上午 / 下午 / 晚上
- 全天
- 暂未确定

最终进入系统的数据应转换为规范 Time 对象。

---

## 4.3 事件简述

用户使用自然语言描述想做的事情。

例如：

```text
打两个小时羽毛球，然后去吃越南菜。
```

用户无需自己填写复杂表单。

AI 后续负责理解其中的：

- 羽毛球
- 时长约 2 小时
- 越南菜
- 事件先后关系

---

## 4.4 创建

点击：

```text
创建行程
```

创建完成后进入行程页面，并允许邀请微信好友加入。

---

# 5. 行程页面

行程页面是整个产品的核心。

主要由以下部分组成：

1. AI 计划大框
2. 邀请好友
3. 我的推荐
4. 大家的想法
5. 需求输入框

---

# 6. AI 计划大框

AI 计划区域展示当前共同计划。

它不是 AI 聊天内容，而是由结构化 Plan 数据渲染出的界面。

示例：

```text
AI 当前计划

11:00 - 13:00
🏸 XX 羽毛球馆
天河区
¥28 / 人

13:00 - 13:40
🚇 前往越秀

14:00
🍜 越南菜

推荐餐厅：

1. Restaurant A
   ¥42 / 人 · 4.7

2. Restaurant B
   ¥51 / 人 · 4.8

3. Restaurant C
   ¥38 / 人 · 4.5

预计总消费：¥75 - ¥95 / 人

已满足 7 / 8 条需求
```

如果存在冲突：

```text
⚠ 当前存在 1 项冲突

D 希望总预算 ≤ ¥70，
但当前最低可执行方案约为 ¥75。
```

---

# 7. 邀请好友

创建者可以通过微信分享邀请其他用户加入行程。

参与者加入后拥有提出需求的权利。

核心原则：

> 创建者负责发起行程，但参与者的需求在 AI 协调时应被平等看待。

创建者保留必要管理权限。

---

# 8. 我的推荐

共同计划回答：

> **我们怎么办？**

“我的推荐”回答：

> **我怎么参与？**

系统根据用户自己的出发位置、时间和公共计划生成个人执行方案。

例如：

```text
我的推荐

10:06 出发

宿舍
 ↓ 步行 8 min
地铁站
 ↓ 地铁 42 min
体育西路
 ↓ 步行 6 min
羽毛球馆

预计 10:57 到达
预计交通费 ¥6
```

不同参与者看到不同的个人路线。

---

# 9. 大家的想法

参与者的需求以评论流形式展示。

例如：

```text
B
我 10 点以后才有空

✓ 已纳入计划
```

```text
C
羽毛球只想在天河打，
吃饭可以去越秀。

✓ 已纳入计划
```

```text
D
最近没什么钱，便宜一点。

✓ 已纳入计划
```

AI 状态建议：

- 已纳入计划
- 正在调整
- 与其他需求冲突
- 暂无法满足
- 等待确认

---

# 10. 需求输入框

底部固定输入框。

推荐文案：

```text
说说你的时间、预算或想法…
```

用户直接输入自然语言。

例如：

```text
我下午五点之前一定要走。
```

系统流程：

```text
Raw Comment
    ↓
Constraint Extraction
    ↓
Plan Update
    ↓
UI Refresh
```

---

# 11. AI 数据原则

## 11.1 AI 不输出页面文案

AI 的核心输出必须是结构化数据。

错误方式：

```json
{
  "plan": "11 点去天河打羽毛球，然后去越秀吃越南菜。"
}
```

正确方向：

```json
{
  "events": [
    {
      "id": "event_001",
      "type": "SPORT",
      "time": {},
      "location": {},
      "price": {}
    }
  ]
}
```

前端根据数据自行渲染 UI。

---

# 12. 核心数据对象

建议至少包含：

```text
Trip
Participant
Comment
Constraint
Plan
Event
Time
Location
Venue
Restaurant
Price
Route
ExternalAction
Notification
```

---

# 13. Comment

保存用户原始输入。

```json
{
  "commentId": "comment_004",
  "userId": "user_D",
  "rawText": "最近没啥钱，能不能便宜一点"
}
```

原始文本必须保留，以便：

- 展示
- AI 重新解析
- 调试
- 审计
- 历史封板

---

# 14. Constraint

AI 将评论解析为结构化约束。

例如：

```json
{
  "constraintId": "constraint_004",
  "sourceCommentId": "comment_004",
  "ownerId": "user_D",
  "type": "BUDGET",
  "scope": "TRIP",
  "value": {
    "preference": "LOW_COST"
  },
  "priority": "SOFT"
}
```

如果用户明确提出：

```text
今天最多花 80 块。
```

则：

```json
{
  "type": "BUDGET",
  "scope": "TRIP",
  "value": {
    "max": 80,
    "currency": "CNY"
  },
  "priority": "HARD"
}
```

---

# 15. 硬约束与软约束

必须区分：

## HARD

原则上不可违反。

例如：

- 10:00 以后才能到
- 17:00 前必须离开
- 预算最多 ¥80
- 羽毛球必须在天河

## SOFT

允许 AI 在无法完全满足时做权衡。

例如：

- 尽量便宜
- 最好坐地铁
- 想吃越南菜
- 不想走太远

---

# 16. Time

时间绝对不能只保存：

```text
下午两点
```

建议结构：

```json
{
  "start": "2026-08-22T14:00:00+08:00",
  "end": "2026-08-22T16:00:00+08:00",
  "timezone": "Asia/Shanghai"
}
```

---

# 17. Location

Location 必须是结构化现实地点。

建议：

```json
{
  "id": "location_001",
  "name": "XX 羽毛球馆",
  "latitude": 23.123456,
  "longitude": 113.123456,
  "address": "广州市天河区……",
  "district": "天河区",
  "city": "广州市",
  "providerRefs": {}
}
```

不能仅保存：

```text
天河体育中心
```

---

# 18. Restaurant

Restaurant 单独作为实体。

```json
{
  "id": "restaurant_001",
  "name": "Restaurant A",
  "location": {},
  "category": [
    "VIETNAMESE"
  ],
  "price": {
    "averagePerPerson": 46,
    "currency": "CNY"
  },
  "rating": {
    "score": 4.7,
    "count": 1253
  },
  "externalActions": []
}
```

---

# 19. Price

Price 不允许仅保存自然语言。

建议支持：

```json
{
  "amount": 46,
  "currency": "CNY",
  "unit": "PER_PERSON"
}
```

或：

```json
{
  "min": 40,
  "max": 60,
  "currency": "CNY",
  "unit": "PER_PERSON"
}
```

---

# 20. Route

路线由地图服务计算，不由 LLM 凭空生成。

```json
{
  "ownerId": "user_A",
  "from": {},
  "to": {},
  "departureTime": "2026-08-22T10:05:00+08:00",
  "arrivalTime": "2026-08-22T10:57:00+08:00",
  "durationMinutes": 52,
  "transport": "METRO",
  "price": {
    "amount": 6,
    "currency": "CNY"
  }
}
```

---

# 21. Provider 层

真实地点与路线不由 AI 创造。

推荐架构：

```text
User Requirements
       ↓
AI Constraint Extraction
       ↓
Planning Engine
       ↓
Provider Layer
├── Tencent Map
├── Dianping / URL
├── Other APIs
└── Future Providers
       ↓
Verified Entity
       ↓
Plan
```

核心原则：

> **LLM 负责理解和选择，Provider 负责真实世界事实。**

AI 不允许直接编造餐厅、球馆、地址、评分或交通路线。

---

# 22. ExternalAction

第三方跳转使用统一 ExternalAction 抽象。

支持：

- URL
- API
- Map
- MiniProgram
- 其他未来方式

示例：

```json
{
  "mode": "URL",
  "provider": "dianping",
  "target": "..."
}
```

或：

```json
{
  "mode": "API",
  "provider": "tencent_map",
  "action": "open_location",
  "params": {
    "latitude": 23.123,
    "longitude": 113.321,
    "name": "XX 羽毛球馆"
  }
}
```

核心原则：

> **AI 输出实体，不输出第三方链接。**

链接、Token、POI ID 等由 Provider / Backend 层管理。

---

# 23. 地点详情页

点击 AI 计划中的地点或餐厅后，可以进入内部地点详情页。

例如：

```text
XX 越南料理

评分：4.7
人均：¥48
区域：越秀区
距离上一地点：4.2 km

AI 推荐理由：
价格符合 D 的预算要求，
同时满足 C 希望在越秀吃饭的要求。

[ 地图 ]
[ 怎么去 ]
[ 大众点评 ]
```

所有地点应尽量可以继续进入地图、导航或第三方详情。

---

# 24. 历史行程

历史页面采用大列表。

列表只展示简洁数据：

- 行程名称
- 创建时间
- 完成时间
- 参与人数
- 总时长
- 人均消费（如果可计算）
- 行程状态

示例：

```text
羽毛球 + 越南菜

创建：2026/08/16 09:42
完成：2026/08/16 17:18

4 人 · 7h36min · ¥86 / 人

已完成
```

---

# 25. 历史详情 / 封板

点击历史行程后进入原行程页面的封板版本。

保留：

- AI 最终计划
- 我的推荐
- 大家的想法
- AI 对需求的处理结果
- 最终路线和地点
- 行程信息

移除所有修改能力：

- 邀请好友
- 输入框
- 修改计划
- 重新规划
- 其他改变行程的按钮

顶部显示：

```text
已完成 · 2026/08/16 17:18 封存
```

历史行程是最终计划的完整快照，而非普通浏览记录。

---

# 26. 微信登录与「我的」

个人页面保持简单。

包含：

- 微信头像
- 微信昵称
- 参与过的行程数量
- 通知设置
- 关于
- 隐私说明

不扩展复杂社交功能。

---

# 27. 通知

通知围绕行程执行。

支持：

- 行程即将开始
- 建议出发时间
- 行程重大调整
- 出现新冲突
- 重要地点或时间发生变化

示例：

```text
该出发啦

距离羽毛球馆预计 47 分钟，
建议现在出发。
```

如果共同计划变化，个人通知时间也应重新计算。

---

# 28. 行程状态机

V1 至少包含：

```text
DRAFT
  ↓
ACTIVE
  ↓
COMPLETED
```

同时支持：

```text
CANCELLED
```

建议定义：

## DRAFT

刚创建，尚未正式进入多人协同。

## ACTIVE

正在规划 / 执行。

允许：

- 邀请
- 评论
- AI 更新
- 冲突处理
- 路线更新

## COMPLETED

行程结束并封板。

禁止进一步修改。

## CANCELLED

行程取消。

保留必要记录。

---

# 29. 权限设计

所有参与者：

- 可以查看共同计划
- 可以提出需求
- 可以查看自己的个人路线
- 可以看到公开评论

创建者额外拥有：

- 邀请管理
- 移除成员
- 取消行程
- 完成 / 封板行程
- 必要管理操作

但：

> **创建者的个人偏好不应天然拥有比其他参与者更高的 AI 权重。**

---

# 30. AI 冲突机制

如果两个 HARD Constraint 无法同时满足：

AI 不允许偷偷忽略其中一个。

必须输出 Conflict。

例如：

```text
B：11:30 后才有空
C：12:00 前必须离开
```

如果事件至少需要 2 小时，则：

```text
CONFLICT
```

UI：

```text
⚠ 当前无法满足所有人的时间要求。

B 11:30 后才能参加，
C 12:00 前必须离开。
```

AI 可提供调整方案，例如：

1. C 不参加羽毛球，只参加吃饭
2. 更换日期
3. 缩短活动时间

最终交由参与者决定。

---

# 31. 建议方案与当前方案

AI 不应该因为任何一句评论都立即破坏已经确定的关键计划。

建议区分：

```text
Suggested Plan
```

与：

```text
Current Plan
```

对于重大变化，例如：

- 更换餐厅
- 更换球馆
- 改变集合时间
- 改变区域
- 明显增加预算

AI 可以先提出建议，再由参与者确认。

轻微调整则可自动更新。

---

# 32. 定位与隐私

“我的推荐”可能需要个人出发地点。

用户应可以：

- 使用当前位置
- 手动选择出发地点
- 不提供位置

原则：

> **个人出发地点默认只用于计算个人路线，不向其他参与者公开。**

共同计划只暴露公共集合地点与公共活动地点。

---

# 33. 失败与降级

系统必须保证第三方服务或 AI 出错时不会导致整个行程不可用。

## AI 服务失败

- 保留上一版 Plan
- 允许继续查看行程
- 评论先保存
- 后续可重新解析

## 地图失败

- 保留 Location 名称
- 保留地址
- 保留已有经纬度
- 暂时隐藏导航

## 点评数据不可用

- 不显示评分
- 不伪造价格
- 不伪造评价

## 重新规划失败

- 保留上一版可执行计划
- 明确告诉用户更新失败

---

# 34. AI 职责边界

AI 可以：

- 理解自然语言
- 提取 Constraint
- 判断 HARD / SOFT
- 分析冲突
- 对真实候选地点进行排序
- 生成调整建议
- 解释为什么推荐某方案

AI 不应该：

- 编造现实地点
- 编造评分
- 编造价格
- 编造路线
- 直接生成第三方 URL
- 自己决定所有重大冲突
- 用纯文本替代结构化数据

---

# 35. V1 非目标

V1 暂不加入：

- 独立 AI 聊天页
- 复杂社交系统
- 好友关系系统
- 行程相册
- 记账系统
- 打卡
- 积分
- 排行榜
- 内容社区
- 攻略社区
- 复杂用户等级
- AI 人格化助手

原则：

> **所有功能都必须服务于“多人零散需求 → 共同计划”。**

否则暂不加入。

---

# 36. V1 页面树

```text
微信登录
   │
   ▼
首页
├── 新建行程
├── 当前行程
├── 推荐
├── 历史行程
└── 我的

新建行程
├── 区域限定
├── 时间范围
├── 事件简述
└── 创建

当前行程
├── AI 当前计划
├── 邀请好友
├── 我的推荐
├── 大家的想法
└── 需求输入

地点详情
├── 基础信息
├── AI 推荐理由
├── 地图
├── 怎么去
└── 第三方详情

历史行程
├── 历史列表
└── 封板详情

我的
├── 微信信息
├── 行程统计
├── 通知授权
└── 隐私 / 关于
```

---

# 37. V1 核心数据流

```text
Create Trip
    ↓
Initial Brief
    ↓
Invite Participants
    ↓
Raw Comments
    ↓
AI Constraint Extraction
    ↓
Constraint Store
    ↓
Conflict Detection
    ↓
Planning Engine
    ↓
Provider Search
    ↓
Verified Location / Venue / Restaurant
    ↓
Structured Plan
    ↓
Frontend Render
    ↓
Personal Route
    ↓
Notification
    ↓
Complete Trip
    ↓
Snapshot / Archive
```

---

# 38. 项目核心创新点

本项目不是普通的 AI 行程规划工具。

传统 AI 行程工具主要解决：

> “帮我规划一个行程。”

本项目解决：

> **“我们每个人都有不同要求，到底怎么形成一个所有人都能执行的共同计划？”**

核心创新：

### 1. 多人自然语言意图协调

多人分别提出碎片需求，由 AI 持续理解和整合。

### 2. 自然语言 → 结构化现实约束

将模糊语言转换为：

- Time
- Location
- Budget
- Availability
- Preference
- Route
- Venue
- Restaurant

等机器可执行数据。

### 3. AI 与真实世界服务结合

AI 不创造现实世界事实，而通过地图、地点、商户等 Provider 获取真实实体。

### 4. 公共计划 + 个人执行

同一个 Trip 同时存在：

```text
我们怎么办
```

与：

```text
我怎么去
```

### 5. 持续维护共同计划

AI 不只是回答一次问题，而是在整个行程生命周期中持续维护当前共识。

---

# 39. 项目核心表达

推荐作为产品设计的长期判断标准：

> **大家负责表达想法，AI 负责把想法变成共同计划。**

也可以表达为：

> **人类共同创造意图，AI 把共同意图变成现实。**

---

# 40. V1 冻结原则

从本版本开始，优先完成核心闭环：

```text
创建
→ 邀请
→ 提需求
→ AI 解析
→ 形成计划
→ 地点可执行
→ 我的路线
→ 通知
→ 完成
→ 历史封板
```

在核心闭环稳定前，不继续增加外围功能。

